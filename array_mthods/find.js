// The find() method returns the first element that satisfies the provided testing function.

// Example:

let numbers = [1, 2, 3, 4];
let foundNumber = numbers.find(number => number > 2);
console.log(foundNumber); // Output: 3