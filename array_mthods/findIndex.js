// The findIndex() method returns the index of the first element that satisfies the provided testing function.

// Example:

let numbers = [1, 2, 3, 4];
let index = numbers.findIndex(number => number > 2);
console.log(index); // Output: 2